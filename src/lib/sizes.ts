import { Size } from "@prisma/client";

/** Display labels for each size enum value. */
export const SIZE_LABELS: Record<Size, string> = {
  S: "S",
  M: "M",
  L: "L",
  XL: "XL",
  XXL: "XXL",
  ONE_SIZE: "One Size",
};
